name: Basic Build report

on:
  push:
    branches:
      - develop
      - feature/*
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
